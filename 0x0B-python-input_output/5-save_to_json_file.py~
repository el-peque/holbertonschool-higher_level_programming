#!/usr/bin/python3
"""Function declaration"""

import json


def from_json_string(my_str):
    """returns an object (Python data structure) represented by a JSON string"""
    return json.dumps(my_str, separators=(',', ': '))
